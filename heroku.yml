# setup部分は必要なし
# setup:
#   addons:
#     - plan: heroku-postgresql
#       as: DATABASE
build:
  docker:
    api: Dockerfile
  config:
    WORKDIR: app
run:
  api: bundle exec puma -C config/puma.rb
